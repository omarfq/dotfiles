- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.zshrc:
    ~/.zshenv:
    ~/.gitconfig: 
    ~/.config/bat:
      create: true
      path: config/bat
    ~/.config/nvim:
      create: true
      path: config/nvim

- create:
    - ~/Work

- shell:
   - 
      command: ./setup_homebrew.zsh
      stdout: true
      stderr: true
