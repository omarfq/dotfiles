- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.zshrc:
    ~/.gitconfig: 

- create:
    - ~/Work

- shell:
    #- [git submodule update --init --recursive, Installing submodules]
    - echo "hey, my name is Prime"
    - sleep 2
    - 
      command: ./test.zsh
      stdout: true
      stderr: true
